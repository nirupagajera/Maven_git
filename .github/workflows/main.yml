# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ,  "BranchX" ]

  pull_request:
    branches: [ "main" ,  "BranchX" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run mabl tests
        uses: mablhq/github-run-tests-action@v1
        env:
          MABL_API_KEY: ${{ secrets.MABL_API_KEY }}
        with:
          plan-labels: Zadoon
          application-id: CkmQWnmbQjcgNXczh7OYzw-a
          environment-id: WdR6MMy57yQhue8ssVTxdw-e

      - name: Notify Slack on Success
        if: success()
        run: |
                  curl -X POST -H "Content-type: application/json" \
                  --data "{\"text\":\"✅ mabl tests PASSED on branch ${GITHUB_REF_NAME}\"}" \
                  • mabl Runs: https://app.mabl.com/workspaces/YOUR_WORKSPACE_ID/runs\"
                  ${{ secrets.SLACK_WEBHOOK_URL }}
                  

      - name: Notify Slack on Failure
        if: failure()
        run: |
                curl -X POST -H "Content-type: application/json" \
                --data "{\"text\":\"❌ mabl tests FAILED on branch ${GITHUB_REF_NAME}\"}" \
                • mabl Runs: https://app.mabl.com/workspaces/YOUR_WORKSPACE_ID/runs\"
                 ${{ secrets.SLACK_WEBHOOK_URL }}

